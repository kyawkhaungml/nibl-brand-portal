import { WelcomeFlow } from '@/components/welcome/welcome-flow';

export const metadata = {
  title: 'Welcome · NiBL Brand Intelligence',
};

export default function WelcomePage() {
  return <WelcomeFlow />;
}
